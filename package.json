{
  "name": "statsplus-roster-limit-report",
  "version": "1.0.0",
  "description": "Pulls roster limits from header row of Statsplus roster page and outputs a list of teams that are over limits.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "nvonbenken@gmail.com",
  "license": "MIT",
  "devDependencies": {
    "@types/puppeteer": "^5.4.2",
    "nodemon": "^2.0.6",
    "ts-node": "^9.1.1",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "puppeteer": "^5.5.0"
  }
}
